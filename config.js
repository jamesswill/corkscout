/* Copy this file to `config.js` and fill in your values.
   Find them in: Supabase Dashboard → Project Settings → API
   The ANON key is safe to ship in the browser — it's protected by the
   Row Level Security policies in schema.sql (public can read, not write).
   `config.js` is gitignored so you don't commit a project-specific file. */
window.SUPABASE_URL = "https://cxspocivvexqhbzyqxzh.supabase.co";
window.SUPABASE_ANON_KEY = "sb_publishable_tdJe1HRnIinAqYZi7949-A_f7_1TZ1h";





