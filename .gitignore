# NOTE: config.js IS committed so the hosted site has the Supabase URL +
# publishable key. That key is meant to live in the browser; the read-only
# RLS policies protect the data. The SECRET key is never here (Actions only).

# node
node_modules/
package-lock.json
npm-debug.log*

# os / editor
.DS_Store
.vscode/
*.log
